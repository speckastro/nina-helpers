version: 2
updates:
  # Actions are pinned to full commit SHAs; Dependabot bumps the SHA and the
  # version comment together, which is the only practical way to keep pins current.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: "Bump"
